[build]
  publish = "."
  command = ""

# Redirect /api/* to an external backend. Replace the target with your backend URL.
[[redirects]]
  from = "/api/*"
  to = "https://your-backend.onrender.com/api/:splat"
  status = 200
  force = true
